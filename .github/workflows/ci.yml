name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  GO_VERSION: "1.24"
  # Pinned rather than "latest": a linter that updates itself turns an unrelated
  # commit red, and the config format is version-specific — v2 rejects a v1 file
  # outright. Keep in sync with GOLANGCI_VERSION in the Makefile.
  GOLANGCI_VERSION: "v2.12.2"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: ${{ env.GOLANGCI_VERSION }}

      # The conformance harness is a separate module, so ./... from the root
      # never reaches it.
      - name: Run golangci-lint on the conformance harness
        uses: golangci/golangci-lint-action@v9
        with:
          version: ${{ env.GOLANGCI_VERSION }}
          working-directory: conformance

  grammar:
    name: Grammar
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Cache ANTLR jar
        uses: actions/cache@v4
        with:
          path: build
          key: ${{ runner.os }}-antlr-${{ hashFiles('Makefile') }}

      # Fails if parser/grammar was hand-edited or if grammar/fhirpath.g4 was
      # changed without regenerating. The generated parser has no other source.
      - name: Verify the parser matches the grammar
        run: make generate-check

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Run tests
        run: go test -v -race -coverprofile=coverage.txt -covermode=atomic ./...

      # The conformance harness is a separate module so that the engine's go.mod
      # stays free of the FHIR model packages.
      - name: Run the conformance harness
        working-directory: conformance
        run: go test -v ./...

      # The harness already fails the run on a regression; this reports the
      # standing conformance number in the job summary.
      - name: Report conformance against the official FHIRPath suite
        if: always()
        working-directory: conformance
        run: |
          go test -run TestOfficialSuite -v . 2>&1 \
            | grep -E "official suite|skipped [0-9]" \
            | sed 's/^[[:space:]]*suite_test.go:[0-9]*: //' \
            | tee -a "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.txt

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test, grammar]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Build
        run: go build -v ./...

      - name: Verify module
        run: go mod verify
